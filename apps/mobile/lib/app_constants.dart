class AppConstants {
  static const String appName = "Notaro";
  static const String apiBaseUrl = "https://api.notaro.app";
  static const Duration connectTimeout = Duration(seconds: 10);
  static const Duration receiveTimeout = Duration(seconds: 10);
}
